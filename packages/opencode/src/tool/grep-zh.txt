- 快速内容搜索工具，适用于任意大小的代码库
- 使用正则表达式搜索文件内容
- 支持完整的正则表达式语法（例如 "log.*Error"、"function\s+\w+" 等）
- 通过 include 参数按模式过滤文件（例如 "*.js"、"*.{ts,tsx}"）
- 返回至少有一个匹配的文件路径和行号，按修改时间排序
- 当需要查找包含特定模式的文件时使用此工具
- 如果需要识别/统计文件内的匹配数量，请直接使用 Bash 工具配合 `rg`（ripgrep）。不要使用 `grep`。
- 当需要开放式搜索，可能需要多轮 glob 和 grep 时，请改用 actor 工具