当在执行过程中需要向用户提问时使用此工具。这允许你：
1. 收集用户偏好或需求
2. 澄清模糊的指令
3. 在处理过程中获取实现选择的决策
4. 向用户提供关于选择方向的选择。

使用说明：
- 当 `custom` 启用时（默认），会自动添加"输入你自己的答案"选项；不要包含"其他"或通用选项
- 答案作为标签数组返回；设置 `multiple: true` 以允许多选
- 如果你推荐某个特定选项，将其设为列表中的第一个选项，并在标签末尾添加"（推荐）"