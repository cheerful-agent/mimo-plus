- 从指定 URL 获取内容
- 接收 URL 和可选格式作为输入
- 获取 URL 内容，转换为请求的格式（默认为 markdown）
- 以指定格式返回内容
- 当需要检索和分析网页内容时使用此工具

使用说明：
  - 重要提示：如果存在其他工具能提供更好的网页获取能力、更适合当前任务或限制更少，应优先使用该工具而非此工具。
  - URL 必须是格式完整的有效 URL
  - HTTP URL 将自动升级为 HTTPS
  - 格式选项："markdown"（默认）、"text" 或 "html"
  - 此工具为只读，不会修改任何文件
  - 如果内容非常大，结果可能会被摘要